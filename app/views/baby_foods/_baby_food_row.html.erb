<tr class="clickable-row" data-href="<%= edit_child_baby_food_path(current_child, baby_food) %>" data-turbo-frame="modal">
  <td class="col-date">
    <span class="font-semibold text-gray-600">時間：</span>
    <%= baby_food.fed_at.strftime("%H:%M") %>
  </td>

  <td class="col-item1">
    <span class="font-semibold text-gray-600">量：</span>
    <%= baby_food.amount %> ml
  </td>

  <td class="col-memo">
    <% if baby_food.memo.present? %>
      <span class="font-semibold text-gray-600">メモ：</span>
      <%= baby_food.memo %>
    <% end %>
  </td>

  <td class="col-item2">
  </td>

 <td class="record-col-chevron text-right pr-2">
    <div class="record-edit-btn">
      <i class="fa-solid fa-chevron-right"></i>
    </div>
  </td>
</tr>