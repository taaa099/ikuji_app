<%= turbo_stream.remove "diaper_#{@diaper.id}" %>

<%= turbo_stream.replace "flash-messages", partial: "shared/flash", locals: { flash: { notice: "おむつ記録を削除しました" } } %>

<%= turbo_stream.update "modal" do %>
  <turbo-frame id="modal"></turbo-frame>
<% end %>