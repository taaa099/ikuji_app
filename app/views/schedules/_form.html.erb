<%= form_with(model: schedule, data: { turbo_frame: "modal" }) do |form| %>
  <div class="schedule-form-container">
    <div class="schedule-form-group">
      <%= form.label :title, class: "schedule-form-label" do %>
        タイトル
        <span class="required-badge">必須</span>
      <% end %>
      <%= form.text_field :title, class: "schedule-form-input #{'input-error' if schedule.errors[:title].any?}"%>
      <% if schedule.errors[:title].any? %>
        <div class="error-message">
          <%= schedule.errors.full_messages_for(:title).join(", ") %>
        </div>
      <% end %>
    </div>

  <div class="schedule-form-group">
    <%= form.label :child_ids, class: "schedule-form-label" do %>
      対象の子供
      <span class="required-badge">必須</span>
    <% end %>

    <!-- ユーザー予定 -->
    <label class="schedule-checkbox-wrapper">
      <%= form.check_box :user_only, { class: "schedule-form-checkbox" }, true, false %>
      <%= "#{current_user.name.presence || 'ユーザー'} 予定として登録" %>
    </label>

    <!-- 子供たち -->
    <%= form.collection_check_boxes :child_ids, current_user.children, :id, :name do |b| %>
      <label class="schedule-checkbox-wrapper">
        <%= b.check_box(class: "schedule-form-checkbox") %>
        <%= b.text %>
      </label>
    <% end %>
    <% if schedule.errors[:child_ids].any? %>
        <div class="error-message">
          <%= schedule.errors.full_messages_for(:child_ids).join(", ") %>
        </div>
      <% end %>
  </div>

    <div class="schedule-form-group">
      <%= form.label :start_time, class: "schedule-form-label" do %>
        開始日時
        <span class="required-badge">必須</span>
      <% end %>
      <%= form.datetime_field :start_time, class: "schedule-form-input #{'input-error' if schedule.errors[:start_time].any?}" %>
      <% if schedule.errors[:start_time].any? %>
        <div class="error-message">
          <%= schedule.errors.full_messages_for(:start_time).join(", ") %>
        </div>
      <% end %>
    </div>

    <div class="schedule-form-group">
      <%= form.label :end_time, class: "schedule-form-label" do %>
        終了日時
        <span class="required-badge">必須</span>
      <% end %>
      <%= form.datetime_field :end_time, class: "schedule-form-input #{'input-error' if schedule.errors[:end_time].any?}" %>
      <% if schedule.errors[:end_time].any? %>
        <div class="error-message">
          <%= schedule.errors.full_messages_for(:end_time).join(", ") %>
        </div>
      <% end %>
    </div>

    <div class="schedule-form-group">
      <%= form.label :all_day, "終日", class: "schedule-form-label" %>
      <%= form.check_box :all_day, class: "schedule-form-checkbox" %>
    </div>

    <div class="schedule-form-group">
      <%= form.label :repeat, "繰り返し", class: "schedule-form-label" %>
      <%= form.select :repeat,
          options_for_select(["none", "daily", "weekly", "monthly", "yearly"], @schedule.repeat),
          {}, class: "schedule-form-input" %>
    </div>

    <div class="schedule-form-group">
      <%= form.label :memo, "メモ", class: "schedule-form-label" %>
      <%= form.text_area :memo, rows: 3, class: "schedule-form-input" %>
    </div>

    <div class="schedule-form-actions">
      <%= form.submit schedule.persisted? ? "更新する" : "登録する", class: "schedule-form-submit" %>
      <a href="#" class="modal-close">閉じる</a>
    </div>
  </div>
<% end %>