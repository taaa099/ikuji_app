<%= turbo_stream.remove "sleep_record_#{@sleep_record.id}" %>
<%= turbo_stream.replace "flash-messages", partial: "shared/flash", locals: { flash: { notice: "睡眠記録を削除しました" } } %>
<%= turbo_stream.update "modal" do %>
  <turbo-frame id="modal"></turbo-frame>
<% end %>